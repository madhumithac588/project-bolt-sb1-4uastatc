import 'package:flutter/material.dart';

// Advice Screen Widget
class AdviceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Advice Screen'),
    );
  }
}
